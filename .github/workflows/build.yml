name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-macos:
    name: macOS (Universal Binary)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install Ninja
        run: brew install ninja

      - name: Cache CPM packages
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/cpm-cache
          key: cpm-macos-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: cpm-macos-

      - name: Configure
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCPM_SOURCE_CACHE=${{ runner.temp }}/cpm-cache

      - name: Build
        run: cmake --build build --parallel

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: DrawwaveVocoder-VST3-macOS
          path: build/DrawwaveVocoder_artefacts/VST3/

      - name: Upload AU
        uses: actions/upload-artifact@v4
        with:
          name: DrawwaveVocoder-AU-macOS
          path: build/DrawwaveVocoder_artefacts/AU/

  build-windows:
    name: Windows x64
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Cache CPM packages
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/cpm-cache
          key: cpm-windows-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: cpm-windows-

      - name: Configure
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release "-DCPM_SOURCE_CACHE=${{ runner.temp }}/cpm-cache"

      - name: Build
        run: cmake --build build --parallel

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: DrawwaveVocoder-VST3-Windows
          path: build/DrawwaveVocoder_artefacts/VST3/
